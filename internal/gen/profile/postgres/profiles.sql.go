// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: profiles.sql

package postgres

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createProfile = `-- name: CreateProfile :one
INSERT INTO profiles (user_id, username)
VALUES ($1, $2)
ON CONFLICT (user_id) DO NOTHING
RETURNING user_id, username, bio, image, created_at, updated_at
`

type CreateProfileParams struct {
	UserID   pgtype.UUID
	Username string
}

func (q *Queries) CreateProfile(ctx context.Context, arg CreateProfileParams) (Profile, error) {
	row := q.db.QueryRow(ctx, createProfile, arg.UserID, arg.Username)
	var i Profile
	err := row.Scan(
		&i.UserID,
		&i.Username,
		&i.Bio,
		&i.Image,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getProfileByID = `-- name: GetProfileByID :one
SELECT user_id, username, bio, image, created_at, updated_at
FROM profiles
WHERE user_id = $1
`

func (q *Queries) GetProfileByID(ctx context.Context, userID pgtype.UUID) (Profile, error) {
	row := q.db.QueryRow(ctx, getProfileByID, userID)
	var i Profile
	err := row.Scan(
		&i.UserID,
		&i.Username,
		&i.Bio,
		&i.Image,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getProfileByUsername = `-- name: GetProfileByUsername :one
SELECT user_id, username, bio, image, created_at, updated_at
FROM profiles
WHERE username = $1
`

func (q *Queries) GetProfileByUsername(ctx context.Context, username string) (Profile, error) {
	row := q.db.QueryRow(ctx, getProfileByUsername, username)
	var i Profile
	err := row.Scan(
		&i.UserID,
		&i.Username,
		&i.Bio,
		&i.Image,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listProfilesByIDs = `-- name: ListProfilesByIDs :many
SELECT user_id, username, bio, image, created_at, updated_at
FROM profiles
WHERE user_id = ANY($1::uuid[])
`

func (q *Queries) ListProfilesByIDs(ctx context.Context, userIds []pgtype.UUID) ([]Profile, error) {
	rows, err := q.db.Query(ctx, listProfilesByIDs, userIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Profile
	for rows.Next() {
		var i Profile
		if err := rows.Scan(
			&i.UserID,
			&i.Username,
			&i.Bio,
			&i.Image,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateProfile = `-- name: UpdateProfile :one
UPDATE profiles
SET username = CASE WHEN $1::boolean THEN $2::text ELSE username END,
    bio = CASE WHEN $3::boolean THEN $4::text ELSE bio END,
    image = CASE WHEN $5::boolean THEN $6::text ELSE image END,
    updated_at = NOW()
WHERE user_id = $7
RETURNING user_id, username, bio, image, created_at, updated_at
`

type UpdateProfileParams struct {
	SetUsername bool
	Username    string
	SetBio      bool
	Bio         pgtype.Text
	SetImage    bool
	Image       pgtype.Text
	UserID      pgtype.UUID
}

func (q *Queries) UpdateProfile(ctx context.Context, arg UpdateProfileParams) (Profile, error) {
	row := q.db.QueryRow(ctx, updateProfile,
		arg.SetUsername,
		arg.Username,
		arg.SetBio,
		arg.Bio,
		arg.SetImage,
		arg.Image,
		arg.UserID,
	)
	var i Profile
	err := row.Scan(
		&i.UserID,
		&i.Username,
		&i.Bio,
		&i.Image,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
