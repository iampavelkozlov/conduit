version: "2"
sql:
  - schema:
      - "migrations/*.sql"
    queries:
      - "migrations/queries/*.sql"
    engine: "postgresql"
    gen:
      go:
        out: "../../internal/gen/auth/postgres"
        package: "postgres"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: false
