db:
  # A separate database is the ownership boundary even when both databases
  # run in the same local PostgreSQL instance.
  dsn: "postgres://postgres:postgres@localhost:5432/subscriptions?sslmode=disable"

logger:
  level: "info"
  format: "text"

grpc:
  address: ":9004"
